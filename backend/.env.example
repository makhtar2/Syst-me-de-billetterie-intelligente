PORT=5050
NODE_ENV=development
MONGO_URI=mongodb://localhost:27017/billetterie

# JWT
JWT_SECRET=changez-moi-en-production
JWT_EXPIRES_IN=7d

# Compte admin initial (npm run seed)
ADMIN_EMAIL=admin@billetterie.com
ADMIN_PASSWORD=admin123

# SMTP (optionnel — sans config, les emails s'affichent dans la console)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=noreply@billetterie.com
